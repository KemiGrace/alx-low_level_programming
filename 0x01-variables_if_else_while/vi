#include <stdio.h>

/**
 * main - Print the alphabet in lower and upper case.
 *
 * Return: Might be 0 (Success)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);

	for (i = 'A'; i <= 'Z'; i++)
		putchar(i);

	putchar('\n');
	return (0);
}

