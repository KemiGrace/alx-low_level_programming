#include "main.h"

/**
 * main - prints the alphabet, in lowercase.
 *
 * Return: no return value
 */

void print_alphabet(void)
{
	char m;
for (m = 'a'; m <= 'z'; m++)
_putchar(m);
_putchar('\n');
}
